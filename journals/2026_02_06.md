-
- **Graph**: nodes + edges + stored text
- **Evaluated graph / reactive graph**: nodes + edges + **rules** that compute values across edges
-
- For your purpose, the most literature-aligned phrasing is:
- > **computed/derived property via relationship traversal**
- If you want vocabulary that will match what engineers recognize immediately: **lookup / rollup / computed property / inheritance (delegation)**.**lookup / rollup / computed property / inheritance (delegation)**.
- A node exposes a **derived child subtree** whose value is obtained by **traversing a relationship edge** to another node and **reading that node’s corresponding subtree**, evaluated dynamically rather than copied.
-
-
- `WKI.disciplines` is **not authored**
- it is **computed by reading WKP.disciplines**
- This notation is perfect for what you’re describing because it explicitly implies **derivation**, not copying